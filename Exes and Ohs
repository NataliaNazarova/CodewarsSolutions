// Exes and Ohs
// https://www.codewars.com/kata/55908aad6620c066bc00002a/train/javascript
function XO(str) {
  let count = 0;
  let str1 = str.toLowerCase();
  for (let el of str1) {
    if (el === 'x') count++;
    if (el === 'o') count--;
  }
  return count === 0;
}
