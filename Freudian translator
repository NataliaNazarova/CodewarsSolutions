// Freudian translator
// https://www.codewars.com/kata/5713bc89c82eff33c60009f7/train/javascript

function toFreud(str) {
  let res = str.split(' ').map(el => el = 'sex').join(' ');
  return str.length === 0 ? str : res;
}
