// Multiply the number
// https://www.codewars.com/kata/5708f682c69b48047b000e07/train/javascript

function multiply(n){
  let s = n + '';
  return n >= 0 ? n * Math.pow(5, s.length) : n * Math.pow(5, s.length - 1);
}
