//Regex validate PIN code
// https://www.codewars.com/kata/55f8a9c06c018a0d6e000132/train/javascript
function validatePIN (pin) {
  let s = pin.replace(/\D/, '');
  return s.length === pin.length && s.length === 4 || s.length === 6 && s.length === pin.length;
}
