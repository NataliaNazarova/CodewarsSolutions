// Sort and Star
https://www.codewars.com/kata/57cfdf34902f6ba3d300001e/train/javascript

function twoSort(s) {
  let arr = s.sort();
  return arr[0].split("").map(el => el = el + '***').join("").slice(0,-3)
}
